---
import type { ComponentProps } from "astro/types";
import Link from "../Link.astro";
import { cleanAstroClasslist } from "../../lib/utils/astro";

type Props = ComponentProps<typeof Link>;

const props = cleanAstroClasslist(Astro.props);
---

<Link {...props}>
  <slot />
</Link>
