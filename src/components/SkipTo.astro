---
interface Props {
  href: `#${string}`;
  name: string;
}

const { href, name } = Astro.props;
---

<a
  class="fixed top-0 left-4 z-50 -translate-y-full bg-neutral-950 p-4 opacity-0 transition focus:translate-y-4 focus:opacity-100"
  href={href}
>
  Saltar a {name}
</a>
